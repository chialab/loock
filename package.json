{
  "name": "@chialab/loock",
  "version": "3.3.0",
  "description": "A focus trap helper for keyboard navigation on the web.",
  "type": "module",
  "main": "dist/cjs/loock.cjs",
  "module": "dist/esm/loock.js",
  "types": "types/index.d.ts",
  "exports": {
    "types": "./types/index.d.ts",
    "require": "./dist/cjs/loock.cjs",
    "default": "./dist/esm/loock.js"
  },
  "files": [
    "dist",
    "types",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chialab/loock"
  },
  "bugs": {
    "url": "https://github.com/chialab/loock/issues"
  },
  "homepage": "https://www.chialab.io/p/loock",
  "author": "Chialab <dev@chialab.io> (https://www.chialab.it)",
  "scripts": {
    "build": "rimraf 'dist' 'types' && yarn types && yarn build:esm && yarn build:cjs",
    "build:esm": "esbuild src/index.js --outfile=dist/esm/loock.js --format=esm --bundle --minify --sourcemap",
    "build:cjs": "esbuild src/index.js --outfile=dist/cjs/loock.cjs --format=cjs --bundle --minify --sourcemap",
    "types": "tsc --declaration --emitDeclarationOnly --declarationDir 'types' --incremental false",
    "test": "yarn vitest",
    "lint": "prettier --check . eslint src",
    "typecheck": "tsc --noEmit",
    "prepack": "yarn build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.24.1",
    "@chialab/eslint-config": "^4.0.0",
    "@chialab/prettier-config": "^1.2.2",
    "esbuild": "^0.19.5",
    "eslint": "^8.0.0",
    "prettier": "^3.0.3",
    "publint": "^0.2.5",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.6"
  }
}
